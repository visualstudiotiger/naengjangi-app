# 디자인 시스템

지금까지 그려온 홈/레시피/장바구니/상점/영수증 촬영 화면에서 실제 사용한 색상·컴포넌트 규칙을 정리한 문서. 디자이너·개발자에게 그대로 전달 가능한 수준을 목표로 함.

## 1. 브랜드 톤

- **민트 그린**을 배경/기본 톤으로 사용 — "신선함"이라는 서비스 본질과 직결
- **코랄**을 포인트 컬러로만 사용 — 캐릭터, 메인 CTA 버튼, 확인이 필요한 상태 표시에 한정
- 두 색을 8:2 비율 정도로 유지 — 코랄이 배경까지 넓게 쓰이면 포인트로서의 기능을 잃음

## 2. 컬러 팔레트

### 배경/뉴트럴

| 이름 | 값 | 용도 |
|---|---|---|
| Background | `#F4F6F1` | 전체 화면 배경 (연한 민트 그레이) |
| Surface | `#FFFFFF` | 카드, 버튼 등 표면 |
| Surface Muted | `#EAF0E2` | 캐릭터 카드 배경, 비활성 탭 배경 |
| Border | `#E1E6D9` | 카드 테두리, 구분선 |
| Border Dashed | `#C7CFBB` | 잠금/미해금 아이템 등 비활성 표시용 점선 |

### 텍스트

| 이름 | 값 | 용도 |
|---|---|---|
| Text Primary | `#2D3327` | 제목, 본문 |
| Text Secondary | `#5F6B54` | 보조 설명, 캡션 |
| Text Muted | `#7A8570` | placeholder, 상태 바 시간 등 |

### 민트 (기본 액션 / 확실한 상태)

| 이름 | 값 | 용도 |
|---|---|---|
| Mint Accent | `#5DCAA5` | 캐릭터 몸통, 진행바 채움 |
| Mint Deep | `#0F6E56` | 액션 아이콘, 활성 탭 텍스트, "재료 다 있어요" 텍스트 |
| Mint BG | `#E1F0E9` | 긍정 배지 배경 ("재료 다 있어요") |

### 코랄 (포인트 액션 / 확인 필요 상태)

| 이름 | 값 | 용도 |
|---|---|---|
| Coral Accent | `#D85A30` | 메인 CTA 버튼, 캐릭터 아이템(리본 등), 콩알 아이콘 |
| Coral BG | `#FBE9E3` | 주의 배지 배경 ("2개 부족", "확인 필요" 안내 박스) |
| Coral Text | `#993C1D` | 주의 배지/안내 텍스트 |

### 캐릭터 전용

| 이름 | 값 | 용도 |
|---|---|---|
| Character Body Light | `#7FDAB8` | 캐릭터 얼굴/머리 부분 (몸통보다 밝게) |
| Character Blush | `#F0997B` (투명도 60%) | 볼터치 |
| Character Eye | `#04342C` | 눈동자 |

## 3. 타이포그래피

- 시스템 기본 폰트 사용 (별도 커스텀 폰트 없음)
- 크기 체계: `11px`(상태바/캡션) / `12px`(보조 텍스트) / `13px`(본문 소) / `14px`(본문 기본, 버튼) / `15px`(리스트 제목) / `16px`(섹션 소제목) / `18px`(화면 타이틀)
- 굵기: 본문은 기본(400), 강조 요소(가격, 카드 제목, 버튼)는 `500`. 굵은(700) 사용 지양 — 톤이 과해짐

## 4. 형태 / 여백

- 카드 모서리: `12~20px` (카드가 클수록 더 둥글게)
- 버튼 모서리: `16px`
- 태그/배지(pill): `10~20px` (완전한 알약 형태는 태그 칩에만 사용)
- 카드 내부 패딩: `12~20px`
- 카드 간 간격: `8~14px`

## 5. 컴포넌트 규칙

### 버튼
- **Primary**: 배경 코랄(`#D85A30`), 텍스트 흰색 — 화면당 1개만 사용 (예: "영수증 업로드")
- **Secondary**: 배경 흰색, 테두리 `#E1E6D9`, 텍스트 `#5F6B54`

### 상태 배지 (신뢰도/재료 상태 공통 규칙)
- **민트 배지**: 확실함, 문제 없음 (예: "재료 다 있어요")
- **회색/뉴트럴**: 애매하지만 표시는 됨
- **코랄 배지**: 확인 필요, 부족함 (예: "2개 부족", 낮은 신뢰도 태그)

이 3색 규칙은 영수증 인식 결과 태그, 레시피 카드 배지, 유통기한 D-day 표시 등 **신뢰도·긴급도를 나타내는 모든 곳에 동일하게 적용**.

### 태그/칩
- 재료 태그는 pill 형태, 상태에 따라 테두리색만 다르게 (민트 테두리/뉴트럴 테두리/코랄 배경)
- 수정 가능한 태그는 우측에 x 아이콘, 추가 태그는 점선 테두리 + "+" 표시

### 카드
- 흰 배경 + `#E1E6D9` 테두리(0.5px)를 기본으로 하고, 그림자는 사용하지 않음 — 톤이 플랫하고 차분한 인상을 유지하기 위함

### 잠금/미해금 상태
- 배경 뉴트럴(`#F4F6F1`), 테두리는 점선(`#C7CFBB`), 자물쇠 아이콘 + 회색 텍스트로 표시

### 하단 탭바
- 활성 아이콘: 민트 딥(`#0F6E56`) / 비활성 아이콘: `#B4BBA9`

## 6. 캐릭터 일러스트 가이드

- 각지지 않은 둥근 도형으로 구성 (원, 타원 위주)
- 몸통은 진한 민트, 얼굴/머리 부분은 밝은 민트로 구분해 입체감 표현
- 눈은 큰 타원 + 작은 흰색 하이라이트로 표정을 단순하지만 생기있게
- 볼터치(코랄, 반투명)로 온기 있는 인상 부여
- 냉장고 모티프를 은유적으로 반영 (예: 몸통 하단에 반투명 사각형으로 냉장고 내부를 암시)
- 착용 아이템(모자, 리본 등)은 캐릭터 위에 겹쳐 그리되, 캐릭터 본체의 비율을 가리지 않도록 크기 절제

## 7. 아이콘

- **Tabler Icons** (outline 스타일) 사용
- 크기: 인라인 아이콘 `16~18px`, 카드 내 강조 아이콘 `20~22px`, 상태 표시용 `13~14px`
- 색상은 위 팔레트의 텍스트/액션 색상을 그대로 따름 (아이콘 전용 색상 별도 정의하지 않음)

## 8. 접근성 메모

- 코랄 배지 텍스트(`#993C1D`)는 코랄 배경(`#FBE9E3`) 위에서 충분한 대비를 확보하도록 유지 — 더 밝은 코랄 톤으로 텍스트를 쓰지 않을 것
- 상태를 색상에만 의존하지 않도록, 배지에는 항상 텍스트(예: "2개 부족")를 함께 표기하여 색약 사용자도 정보를 인지할 수 있게 함
